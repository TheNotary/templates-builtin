name: CI - Ruby CLI

on:
  push:
    branches: [main]
    paths:
      - 'ruby-cli/**'
      - '.github/workflows/ci-ruby-cli.yml'
  pull_request:
    branches: [main]
    paths:
      - 'ruby-cli/**'
      - '.github/workflows/ci-ruby-cli.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ruby-cli

    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'

      - name: Initialize git repo for gemspec
        run: |
          git init
          git add .

      - name: Install dependencies
        run: bundle install

      - name: Run tests
        run: rake spec
